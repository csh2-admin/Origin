services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT:-5432}
      - DB_NAME=${DB_NAME:-csh2-database}
      - DB_SSLMODE=${DB_SSLMODE:-require}
