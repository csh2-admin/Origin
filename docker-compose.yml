services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      - FLASK_APP=backend.app.main:app
    volumes:
      - uploads:/app/backend/uploads

volumes:
  uploads:
